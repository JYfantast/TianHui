#!/bin/bash

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="choice"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="QA"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="RC"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="NER"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="MC"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="recommend"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="instructions"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="chemical"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="pharmacological"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="abstract"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="title"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"

# 设置参数
model_type="14B.128.256.0.4.4"
question_type="acupoint"

# 运行 Python 脚本并传入参数
python 2025-03-21.py --model_type "$model_type" --question_type "$question_type"
